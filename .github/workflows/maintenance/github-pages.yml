# Deploy doxygen generated documentation to GitHub Pages site
name: Generate doxygen

on:
  workflow_call:
    inputs:
      deploy:
        description: "Set to true to deploy documentation"
        required: false
        default: false
        type: boolean
  push:
    branches:
      - "main"
      - "release/*"
    paths:
      - '.github/workflows/maintenance/github-pages.yml'
      - 'doc/doxyfile.in'
      - 'doc/src/**'
      - 'cmake/**'
      - 'CMakeLists.txt'
  pull_request:
    branches: ["main", "release/*"]
    paths:
      - '.github/workflows/maintenance/github-pages.yml'
      - 'doc/doxyfile.in'
      - 'doc/src/**'
      - 'cmake/**'
      - 'CMakeLists.txt'
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Doxygen
        run: sudo apt-get update && sudo apt-get install -y doxygen

      - name: Generate doxyfile
        run: cmake -P cmake/docs.cmake

      - name: Build doxygen
        run: doxygen doxyfile
        working-directory: doc

      - name: Setup Pages
        if: inputs.deploy
        uses: actions/configure-pages@v5

      - name: Upload artifact
        if: inputs.deploy
        uses: actions/upload-pages-artifact@v4
        with:
          # Upload html subfolder generated in doc
          path: 'doc/api/html'

  # Deployment job
  deploy:
    if: inputs.deploy
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

