@PACKAGE_INIT@

# Thread override fix — early enough to affect FindThreads in dependencies
include("${CMAKE_CURRENT_LIST_DIR}/vanillapdfConfigCommon.cmake")

# Resolve dependencies
include("${CMAKE_CURRENT_LIST_DIR}/vanillapdfDependencies.cmake")

# Load targets
include("${CMAKE_CURRENT_LIST_DIR}/vanillapdfTargets.cmake")
